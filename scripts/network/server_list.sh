#############
# This file defines names for servers and their credentials. This makes it convenient for use with SSH functions. 
# Instead of typing in the full details we just use the name. The order of definition is (ip, username, password)
#############

#### Local #####
declare -a servers_localhost=('localhost' "kshitiz" "a");


########## Functions defined below this point have been automatically added by save_server() #########
