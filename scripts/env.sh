### Provides function to permanently set environment variable direct from the command line without editing a file

# Saves a variable by appending it to the end of this file. Adds a comment before the variable to indicate when it was saved and by whom.
# Usage: save_env variablename
save_env() {
	:
}